# Build-only compose file — builds all extraction container images
# Usage: docker compose -f docker/docker-compose.build.yml build
#
# These containers are NOT long-running services. They are invoked
# per-attachment by sandbox.py with strict security constraints.

services:
  magic:
    build:
      context: .
      dockerfile: magic.Dockerfile
    image: attijari-extract-magic

  oletools:
    build:
      context: .
      dockerfile: oletools.Dockerfile
    image: attijari-extract-oletools

  pdfid:
    build:
      context: .
      dockerfile: pdfid.Dockerfile
    image: attijari-extract-pdfid

  pymupdf:
    build:
      context: .
      dockerfile: pymupdf.Dockerfile
    image: attijari-extract-pymupdf

  tesseract:
    build:
      context: .
      dockerfile: tesseract.Dockerfile
    image: attijari-extract-tesseract

  yara:
    build:
      context: .
      dockerfile: yara.Dockerfile
    image: attijari-extract-yara

  ioc-finder:
    build:
      context: .
      dockerfile: ioc_finder.Dockerfile
    image: attijari-extract-ioc-finder

  markitdown:
    build:
      context: .
      dockerfile: markitdown.Dockerfile
    image: attijari-extract-markitdown

  archive:
    build:
      context: .
      dockerfile: archive.Dockerfile
    image: attijari-extract-archive
