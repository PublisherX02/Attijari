# Core framework
fastapi==0.137.2
uvicorn==0.49.0
pydantic==2.13.4
python-dotenv==1.2.2
requests==2.34.2
python-multipart==0.0.32
Jinja2==3.1.6
urllib3==2.7.0
websockets==12.0            # WS client for the dashboard->websockify VNC relay

# Database
psycopg2-binary==2.9.12
SQLAlchemy==2.0.41

# Rate limiting
slowapi==0.1.9

# Authentication & security
bcrypt==5.0.0
PyJWT==2.13.0
pyotp==2.10.0
cryptography==48.0.1

# Monitoring & scheduling
prometheus-client==0.22.1
APScheduler==3.11.0

# Utilities
filelock==3.29.4

# Shared state store (ARCH-1) — rate limits, TOTP replay cache, detonation
# window lock, pipeline scan lock. Memurai for local dev, real Redis in prod.
redis==8.0.1

# Kubernetes API client — extraction sandbox as a managed pool of Jobs on
# the on-prem deployment target (SANDBOX_BACKEND=kubernetes). Unused on
# local dev, where SANDBOX_BACKEND defaults to "docker".
kubernetes==36.0.3

# RQ — per-email job queue on Redis/Memurai (replaces the poll-and-batch loop)
rq==2.10.0

# Email parsing & auth
dkimpy==1.1.8
checkdmarc==5.17.4
dnspython==2.6.1

# Inbound SMTP server (replaces IMAP polling)
aiosmtpd==1.4.6

# File type detection
python-magic==0.4.27; sys_platform != "win32"
python-magic-bin==0.4.14; sys_platform == "win32"

# Extraction & analysis tools (Stage 3 — isolated extraction)
oletools==0.60.2
pdfid
pymupdf==1.27.2.3
pytesseract==0.3.13
Pillow==12.3.0
opencv-python==5.0.0.93  # QR code ("quishing") decoding via QRCodeDetector -- runs locally, same pattern as pytesseract above
numpy==2.2.6             # opencv-python's own array-buffer dependency
yara-python==4.5.4
pycdlib==1.16.0   # ISO9660/Joliet disk-image member extraction
py7zr==1.1.3      # real 7z archive member extraction (zipfile can't open these)
LnkParse3==1.6.0  # parse a shortcut's real payload -- its command line, not the .lnk bytes
ioc-finder==9.4.1
git+https://github.com/microsoft/markitdown.git#subdirectory=packages/markitdown

# Threat intelligence & enrichment
OTXv2
whoisit
dnstwist[full]

# Google API (optional)
google-api-python-client==2.197.0
google-auth-httplib2==0.4.0
google-auth-oauthlib==1.4.0

# LLM runtime
ollama

# Semantic prompt guard (Llama Guard 3)
# torch is pinned to the newest CUDA build PyTorch publishes (cu132) that this
# machine's driver supports (nvidia-smi reports CUDA 13.3; the RTX 4060's Ada
# Lovelace architecture is fully supported) so Llama Guard inference actually
# runs on GPU. The plain PyPI index only hosts CPU-only Windows wheels for
# torch; --extra-index-url pulls the +cu132 build without replacing the
# default index for every other package in this file.
--extra-index-url https://download.pytorch.org/whl/cu132
transformers==5.12.1
torch==2.13.0+cu132
accelerate==1.14.0  # required by transformers pipeline(..., device_map="auto") in analysis.py

